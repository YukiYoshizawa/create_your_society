<h1><%= @society.title %></h1>
<%=@society.introduction %>

オーナー
<%= @society.owner.name %>

メンバー

<% if @society.users.blank? %>
  参加しているメンバーはまだいません...
<% else %>
  <% @society.users.each do |user| %>
    <%= user.name %>
  <% end %>
<% end %>

[参加ボタンなど]
<% if user_signed_in? %>
  <% if @society.user_societies.exists?(user_id: current_user.id) %>
    <%= link_to "脱退する",  society_membership_path(@society), method: :delete %>
  <% else %>
    <%= link_to "参加する", society_membership_path(@society), method: :post%>
  <% end %>
<% end %>