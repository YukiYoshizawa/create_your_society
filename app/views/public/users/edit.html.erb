<div class="text-center">
  <h1>ユーザー情報の変更</h1>
  <%= form_with model: @user, url: users_information_path do |f| %>
    <%# <%= render "public/shared/error_messages", resource: resource %> 

    <div class="field">
      <%= f.label :"ユーザーネーム" %><br>
      <%= f.text_field :name, maxlength: 8 ,size: "30x10", placeholder: "8文字まで" %>
    </div>  

    <div class="field">
      <p class="mt-2 mb-2 ">変更前のプロフィール画像
        <%= image_tag @user.get_profile_image(100,100) %>
      </p>
      <p><label for="inputImage">プロフィール画像を変更</label>
        <%= f.file_field :profile_image, placeholder: "プロフィール画像", accept: "image/*" %> 
      </p>
    </div>

    <div class="field">
      <%= f.label :"メールアドレス" %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", size: "30x10" %>
    </div>

    <div class="field">
      <%= f.label :"パスワード6文字以上" %>
      <% if @minimum_password_length %>
      <em>(<%= @minimum_password_length %> 文字以上)</em>
      <% end %><br />
      <%= f.password_field :password, autocomplete: "new-password" %>
    </div>

    <div class="field">
      <%= f.label :"パスワードの確認" %><br />
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
    </div>

    <div class="actions mt-3">
      <%= f.submit "更新", class: "btn btn-outline-success" %>
    </div>
  <% end %>
</div>