<!DOCTYPE html>
<html>
  <head>
    <title>CreateYourSociety</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <script src="https://kit.fontawesome.com/ed90dd2909.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="navbar navbar-expand-md navbar-dark bg-dark mb-4">
      <%= link_to "ロゴ", root_path, class: "navbar-brand" %>
      <div class="collapse navbar-collapse">
        <% if admin_signed_in? %>
          <%= render "layouts/admin_header" %>
        <% else %>
          <%= render "layouts/user_header" %>
        <% end %>
      </div>
    </header>
    <div class="container h-auto">
      <%= yield %>
    </div>
  </body>
</html>
